# NOMAD_RC5 test helpers

This folder keeps the legacy Gym/JAX RC5 environment and plotting helpers.

Use it for offline comparisons against simax controllers such as PID or MPC.
The training path uses `NOMAD_RC5.env.RC5TorchBatch` and
`NOMAD_RC5.env.RC5TorchVecEnv` instead.
